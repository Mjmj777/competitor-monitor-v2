V8 HOTFIX — missing intelligence.json

Upload these two files to the same paths in your GitHub repository and replace the existing files:

1) generate_ai_summary.py
2) .github/workflows/monitor.yml

The workflow now runs:
monitor.py -> build_intelligence_metrics.py -> generate_ai_summary.py -> deploy

There is also a fallback in generate_ai_summary.py: if intelligence.json is missing, it builds it automatically before calling OpenAI.

After committing, open Actions -> Competitor Intelligence Monitor -> Run workflow.
For the first run, you may enable force_ai=true to regenerate the V8 AI package immediately.
